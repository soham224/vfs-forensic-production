# =============================================================================
# VFS Forensic Video Notifier Service - Production Environment Configuration
# =============================================================================
# This file contains environment variables for the video notifier service in
# production/Docker deployment mode. The notifier monitors video directories
# and processes incoming surveillance footage.
#
# Author: VFS Team
# Environment: Production/Docker
# =============================================================================

# -----------------------------------------------------------------------------
# Logging Configuration
# -----------------------------------------------------------------------------
# Base directory for storing application logs
# In Docker: This should be mapped to a volume for log persistence
LOG_BASE_DIRECTORY=/app/logs

# -----------------------------------------------------------------------------
# MySQL Database Configuration (Dockerized)
# -----------------------------------------------------------------------------
# Database connection settings for video metadata and forensic case data
# NOTE: If MySQL is running in a Docker container, use the service name 'mysql'
#       If MySQL is on the host, use 'host.docker.internal' or the host IP

MYSQL_USER="root"                          # Database user with read/write privileges
MYSQL_PASS="Rootmysql@123"                # Database user password (production password)
MYSQL_HOST="mysql"                         # MySQL Docker service name or hostname
MYSQL_PORT="3306"                          # MySQL server port (default: 3306)
MYSQL_DB_NAME="vfs_auto_serving"          # Target database name (must match backend DB)

# -----------------------------------------------------------------------------
# Video File System Paths (Absolute Host Paths)
# -----------------------------------------------------------------------------
# IMPORTANT: These paths are on the host machine and should be mounted as
# Docker volumes for the containerized application to access them

# Directory where client surveillance cameras upload video files
# This is monitored continuously for new incoming video footage
CLIENT_VIDEO_DIR=/home/dev56/Soham/Client_video

# Internal directory path where processed videos are stored permanently
# Videos are moved/organized here after initial processing
INTERNAL_VIDEO_FOLDER_PATH=/home/dev56/Soham/frames/vfs/videos

# Root directory that NGINX serves video files from
# Must have proper read permissions configured for NGINX process
NGINX_SERVE_DIRECTORY=/home/dev56/Soham/frames/vfs/videos

# Base filesystem path on the host machine
# Used for constructing absolute paths and path transformations
BASE_PATH=/home/dev56

# -----------------------------------------------------------------------------
# Video Processing Configuration
# -----------------------------------------------------------------------------
# Interval (in seconds) for polling video directories for new files
# Recommended: 5-10 seconds for production to balance responsiveness and CPU usage
FOLDER_POLL_INTERVAL=5

# -----------------------------------------------------------------------------
# NGINX Static File Serving Configuration (No-Copy Mode)
# -----------------------------------------------------------------------------
# Base URL where NGINX serves the video files to end users
# Format: http://hostname:port/path
# Videos will be accessible at: NGINX_BASE_URL/relative_video_path
# Example: http://192.168.11.87:7000/videos/2024/01/15/camera1_recording.mp4
NGINX_BASE_URL=http://192.168.11.87:7000/videos
